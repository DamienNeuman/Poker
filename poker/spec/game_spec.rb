require 'game'

describe '#initialize' do
  it " creates a new game with a deck" do
    expect(Deck.new).to eq (deck)
  it "starts pot size a 0" do

  it "creates 4 players" do

  it "prevents more than 3 cards to be replaced in a players hand"

  it "game deals 5 cards to each player at the beginning of the game" 
